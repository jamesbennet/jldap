* HTTP integration tests.
* Validation of JSON against schema
* Health check endpoint?
* Password hashing (+salt) instead of plaintext userPassword(s) at rest on the disk. Will need API/UI changes to set/change passwords.
* A self-service (non-admin) “change my password” API/UI endpoint.
* Improve performance with large directories / bulk operations.
* Support more filter types.
* Audit Logging
* Rate Limiting/Lockout
* Implement Modify/Add/Delete via LDAP (write operations) properly.
* Rewrite the UI in general.